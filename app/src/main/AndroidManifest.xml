<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DogHealth">

        <!-- Ekran za random psa -->
        <activity
            android:name="com.nikola.doghealth.ui.random.RandomDogActivity"
            android:exported="false" />

        <!-- Detalji rase -->
        <activity
            android:name="com.nikola.doghealth.ui.detail.BreedDetailActivity"
            android:exported="false" />

        <!-- Launcher ekran -->
        <activity
            android:name="com.nikola.doghealth.ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>